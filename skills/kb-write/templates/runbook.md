# <Project Name> — Runbook

> How to run it. Commands, env vars, ports, procedures.

## Prerequisites

<!-- What needs to be installed, configured, or running before starting. -->

## Quick Start

```bash
# Clone and setup
git clone <repo-url>
cd <project-dir>

# Install dependencies
<!-- e.g. pip install -e ., npm install, etc. -->

# Run
<!-- e.g. python -m server.main, npm start, etc. -->
```

## Environment Variables

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| <!-- VAR_NAME --> | <!-- yes/no --> | <!-- value --> | <!-- what it does --> |

## Ports

| Service | Port | Purpose |
|---------|------|---------|
| <!-- e.g. API server --> | <!-- 8000 --> | <!-- REST API --> |

## Common Operations

### Running tests

```bash
<!-- test command -->
```

### Database migrations

```bash
<!-- migration command -->
```

### Debugging

```bash
<!-- useful debug commands: logs, status checks, etc. -->
```

## Troubleshooting

| Symptom | Likely cause | Fix |
|---------|-------------|-----|
| <!-- e.g. "Port already in use" --> | <!-- e.g. "Stale process" --> | <!-- e.g. "kill -9 $(lsof -ti:8000)" --> -->
